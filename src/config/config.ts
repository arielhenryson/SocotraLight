export const config = {
	appName: "Socotra",
	domain: "localhost",
	version: "0.0.1",
	httpServer:true,
	httpPort: 3000,
	maxAge: 86400000,
	serveStaticFiles: true,
    compression: true,
	development: true,
};